# 📘 توثيق الباكإند (Backend Documentation)

هذا المستند يشرح بالتفصيل كيفية عمل نظام المصادقة (Authentication) في الباكإند، بما في ذلك التسجيل، تسجيل الدخول، استعادة كلمة المرور، والتكامل مع Google.

## 🛠️ التقنيات المستخدمة

- **Node.js & Express**: لبناء السيرفر والـ API.
- **MongoDB & Mongoose**: لقاعدة البيانات ونمذجة البيانات (Data Modeling).
- **JWT (JSON Web Tokens)**: لتأمين الجلسات وإدارة تسجيل الدخول.
- **Bcrypt.js**: لتشفير كلمات المرور (Hashing).
- **Nodemailer**: لإرسال رسائل البريد الإلكتروني (رموز التحقق).
- **Dotenv**: لإدارة متغيرات البيئة.

---

## 🔐 مميزات المصادقة (Authentication Features)

يحتوي ملف `auth.controller.js` على الدوال الأساسية التالية:

### 1. تسجيل حساب جديد (`register`)
عندما يقوم المستخدم بإنشاء حساب جديد، يقوم النظام بالخطوات التالية:
1. **التحقق (Validation)**:
   - التأكد من وجود البريد الإلكتروني، كلمة المرور، وتأكيد كلمة المرور.
   - التأكد من تطابق كلمتي المرور.
   - التأكد من أن البريد الإلكتروني غير مسجل مسبقاً.
2. **إنشاء رمز التحقق**:
   - توليد رمز عشوائي مكون من 6 أرقام.
   - تحديد صلاحية الرمز لمدة 10 دقائق.
3. **تشفير كلمة المرور**: استخدام `bcrypt` لتشفير كلمة المرور قبل حفظها.
4. **حفظ المستخدم المؤقت**: يتم حفظ المستخدم في قاعدة البيانات مع حالة `isVerified: false`.
5. **إرسال البريد الإلكتروني**: إرسال رمز التحقق إلى بريد المستخدم باستخدام `nodemailer`.

### 2. تفعيل الحساب (`verifyEmail`)
لتأكيد أن المستخدم يمتلك البريد الإلكتروني:
1. يستقبل النظام البريد الإلكتروني ورمز التحقق.
2. يبحث عن المستخدم ويطابق الرمز المرسل مع الرمز المخزن.
3. يتحقق من أن الرمز لم تنتهي صلاحيته.
4. إذا صح كل شيء، يتم تحويل حالة المستخدم إلى `isVerified: true` ويتم مسح رمز التحقق.

### 3. تسجيل الدخول (`login`)
للدخول إلى الحساب المفعل:
1. يتحقق من وجود المستخدم.
2. يقارن كلمة المرور المدخلة مع الكلمة المشفرة المخزنة.
3. إذا كانت صحيحة، يقوم بإنشاء **JWT Token** يحتوي على `id` و `email` المستخدم.
4. مدة صلاحية التوكن هي 7 أيام.

### 4. نسيان كلمة المرور (`forgotPassword`)
إذا نسي المستخدم كلمة المرور:
1. يبحث النظام عن المستخدم بالبريد الإلكتروني.
2. يولد رمز استعادة جديد (مشفر بـ SHA256 في قاعدة البيانات للأمان).
3. يرسل الرمز (الأصلي) إلى بريد المستخدم.
4. صلاحية الرمز 15 دقيقة.

### 5. إعادة تعيين كلمة المرور (`resetPassword`)
بعد استلام الرمز:
1. يبحث النظام عن المستخدم الذي لديه هذا الرمز (بعد التشفير) والبريد الإلكتروني.
2. يتحقق من صلاحية الوقت.
3. يقوم بتشفير كلمة المرور الجديدة وتحديثها في قاعدة البيانات.
4. يمسح رمز الاستعادة.

### 6. تسجيل الدخول بواسطة جوجل (`googleLogin`)
هذه ميزة تم إضافتها حديثاً وتعمل كالتالي:
1. يستقبل الباكإند `accessToken` من الفرونت إند (الذي حصل عليه من جوجل).
2. **التحقق المباشر**: يقوم الباكإند بإرسال طلب إلى Google API (`userinfo`) للتأكد من صحة التوكن والحصول على بيانات المستخدم.
3. **التسجيل/الدخول التلقائي**:
   - إذا كان المستخدم موجوداً، يتم تسجيل دخوله.
   - إذا كان جديداً، يتم إنشاؤه فوراً مع `isVerified: true` (لأن جوجل جهة موثوقة).
4. **إصدار التوكن**: يتم إرجاع JWT Token خاص بتطبيقنا للفرونت إند.

---

## 🗄️ نموذج قاعدة البيانات (Parent Model)

يخزن بيانات الآباء ويشمل الحقول:
- `name`: الاسم.
- `email`: البريد الإلكتروني (فريد).
- `password`: كلمة المرور (مشفرة).
- `isVerified`: حالة تفعيل الحساب (Boolean).
- `verificationCode` & `verificationCodeExpires`: للتحقق من الإيميل.
- `resetCode` & `resetCodeExpires`: لاستعادة كلمة المرور.
- `provider`: لتحديد نوع الحساب (`google` أو فارغ للحساب العادي).

---
