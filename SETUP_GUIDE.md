# 🚀 تطبيق ADHD - دليل التشغيل والتكامل مع Google OAuth

## 📋 المحتويات

- [متطلبات المشروع](#متطلبات-المشروع)
- [خطوات الإعداد](#خطوات-الإعداد)
- [إعداد Google OAuth](#إعداد-google-oauth)
- [تشغيل التطبيق](#تشغيل-التطبيق)
- [الميزات](#الميزات)

---

## 📦 متطلبات المشروع

### المتطلبات:

- **Node.js** v16 أو أعلى
- **npm** أو **yarn**
- **MongoDB** (محلي أو Neon Database)
- حساب **Google Cloud**

### التحقق من التثبيت:

```bash
node --version
npm --version
```

---

## ⚙️ خطوات الإعداد

### 1. استنساخ المشروع

```bash
cd "f:\Web Development MERN Full Stack\Gradution Project\Dr.Abdullah Project\Namaa Project\ADHD"
```

### 2. إعداد الباكإند

```bash
cd backend

# تثبيت المكتبات
npm install

# إنشاء ملف .env
# أضف المتغيرات التالية:
# PORT=5000
# MONGODB_URI=your_database_url
# JWT_SECRET=your_secret_key
# EMAIL_USER=your_email
# EMAIL_PASS=your_password

# تشغيل الخادم
npm run dev
```

### 3. إعداد الفرونت إند

```bash
cd frontend

# تثبيت المكتبات
npm install

# التحقق من ملف .env
# يجب أن يحتوي على:
# VITE_API_BASE_URL=http://localhost:5000/api/auth
# VITE_GOOGLE_CLIENT_ID=your_google_client_id

# تشغيل التطبيق
npm run dev
```

---

## 🔐 إعداد Google OAuth

### خطوة 1: إنشاء Google Cloud Project

1. اذهب إلى [Google Cloud Console](https://console.cloud.google.com/)
2. أنشئ مشروع جديد:
   - اضغط على اسم المشروع في الأعلى
   - اختر "New Project"
   - أدخل اسم المشروع
   - اضغط "Create"

### خطوة 2: تفعيل Google+ API

1. من Dashboard، اذهب إلى "APIs & Services" > "Library"
2. ابحث عن "Google+ API"
3. اضغط "Enable"

### خطوة 3: إنشاء OAuth 2.0 Credentials

1. اذهب إلى "APIs & Services" > "Credentials"
2. اضغط "Create Credentials" > "OAuth Client ID"
3. اختر "Web application"
4. في الحقل "Authorized redirect URIs" أضف:
   - `http://localhost:3000`
   - `http://localhost:5173` (للتطوير)
   - عنوانك النهائي (للإنتاج)
5. انسخ **Client ID**

### خطوة 4: إضافة Google Client ID للتطبيق

قم بتحديث ملف `.env` في المجلد `frontend`:

```dotenv
VITE_API_BASE_URL=http://localhost:5000/api/auth
VITE_GOOGLE_CLIENT_ID=your_copied_client_id_here
```

---

## 🚀 تشغيل التطبيق

### تشغيل الباكإند:

```bash
cd backend
npm run dev
# الخادم سيعمل على http://localhost:5000
```

### تشغيل الفرونت إند (في terminal جديد):

```bash
cd frontend
npm run dev
# التطبيق سيعمل على http://localhost:5173
```

### الوصول للتطبيق:

افتح المتصفح وانتقل إلى: `http://localhost:5173`

---

## ✨ الميزات

### تسجيل الدخول

- ✅ تسجيل الدخول برقم البريد الإلكتروني وكلمة المرور
- ✅ تسجيل الدخول باستخدام حساب Google
- ✅ استعادة كلمة المرور

### إنشاء الحساب

- ✅ إنشاء حساب جديد بالبريد الإلكتروني والمرور
- ✅ التحقق من البريد الإلكتروني برمز التحقق
- ✅ إنشاء حساب باستخدام Google (تلقائياً)

### Google OAuth

- ✅ تسجيل دخول آمن مع Google
- ✅ إنشاء حساب تلقائي من بيانات Google
- ✅ حفظ البيانات في قاعدة البيانات
- ✅ إرسال JWT Token للعميل

---

## 🔧 استكشاف الأخطاء

### المشكلة: "Invalid Google token"

**الحل:**

- تأكد من أن Google Client ID صحيح في `.env`
- تأكد من أن Google+ API مُفعّلة
- أعد تحميل الصفحة

### المشكلة: CORS Error

**الحل:**

- تأكد من أن الخادم يعمل على `http://localhost:5000`
- تأكد من أن CORS مفعّل في الباكإند

### المشكلة: "قاعدة البيانات غير موجودة"

**الحل:**

- تأكد من أن MongoDB يعمل
- تأكد من أن `MONGODB_URI` صحيح في `.env`

### المشكلة: "كلمة السر غير متطابقة"

**الحل:**

- تأكد من أن كلمات المرور متطابقة في حقول الإدخال
- استخدم كلمة مرور قوية (أكثر من 6 أحرف)

---

## 📊 بنية المشروع

```
ADHD/
├── backend/
│   ├── src/
│   │   ├── app.js
│   │   ├── config/
│   │   │   ├── db.js
│   │   │   └── mailer.js
│   │   ├── controllers/
│   │   │   └── auth.controller.js
│   │   ├── middleware/
│   │   │   └── auth.middleware.js
│   │   ├── models/
│   │   │   └── Parent.js
│   │   └── routes/
│   │       └── auth.routes.js
│   ├── .env
│   └── package.json
│
└── frontend/
    ├── src/
    │   ├── App.tsx
    │   ├── main.tsx
    │   ├── components/
    │   │   └── ui/
    │   │       ├── Button.tsx
    │   │       ├── Input.tsx
    │   │       └── Logo.tsx
    │   ├── context/
    │   │   └── AuthContext.tsx
    │   ├── pages/
    │   │   └── auth/
    │   │       ├── LoginScreen.tsx
    │   │       ├── SignupScreen.tsx
    │   │       └── ...
    │   ├── services/
    │   │   ├── api.ts
    │   │   ├── authApi.ts
    │   │   └── request.ts
    │   └── types/
    │       └── index.ts
    ├── .env
    └── package.json
```

---

## 📖 المراجع

- [Google OAuth 2.0 Documentation](https://developers.google.com/identity/protocols/oauth2)
- [@react-oauth/google Documentation](https://www.npmjs.com/package/@react-oauth/google)
- [Express.js Documentation](https://expressjs.com/)
- [MongoDB Documentation](https://docs.mongodb.com/)

---

## 💡 نصائح

1. **التطوير**: استخدم `npm run dev` لتشغيل خادم التطوير مع Hot Reload
2. **الإنتاج**: استخدم `npm run build` لبناء النسخة الإنتاجية
3. **قاعدة البيانات**: احفظ نسخة احتياطية بانتظام
4. **الأمان**: لا تشارك Google Client ID أو JWT Secret

---

## 📞 الدعم

إذا واجهت مشاكل:

1. تحقق من ملفات `.env`
2. تأكد من عمل جميع الخدمات
3. اقرأ رسائل الخطأ بعناية
4. راجع [GOOGLE_SETUP.md](./GOOGLE_SETUP.md) للمزيد من المعلومات

---

## ✅ تم الإعداد بنجاح!

استمتع بتطبيقك! 🎉
